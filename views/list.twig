<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Books</title>
</head>
<body>
    <table border="1">
        <thead>
            <th> ID </th>
            <th> Title </th>
            <th> Authors </th>
            <th> Preview </th>
        </thead>
        <tbody>
            {% for book in books %}
            <tr>
                <td> {{ book.id }} </td>
                <td> {{ book.title }} </td>
                <td> {{ book.authors }} </td>
                <td> <a href="/books/detail/{{book.id}}">Link</a> </td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
</body>
</html>